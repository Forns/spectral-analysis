/**
 * index-display.js
 * 
 * Manages display elements on the index view.
 */

$(function () {
  
  // Set up the click functionality for the submit button
  $("#submit-button").click(function () {
    alert($("#data-entry").val());
  });
  
});
